name: Trying out my first Javascript GitHub Action

on:
  push:
    branches: [ main ]

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: My JS action
        uses: ArianJC2002/hello-world-javascript-action@v1.3
        with:
          who-to-greet: Mona the Octocat